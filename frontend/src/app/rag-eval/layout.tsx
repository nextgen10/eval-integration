'use client';

import React from 'react';

/**
 * RAG Eval layout - placeholder for future structure unification.
 * Page retains inline nav due to view-specific actions (Export, Evaluate).
 */
export default function RAGEvalLayout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
